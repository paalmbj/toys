import numpy

READ_LENGTH = 126
DIM1_LENGTH = 

dt = numpy.dtype('a{0}'.format(READ_LENGTH))


# 1,2
# 1,4
# 1,11



NCOL = 50000

last_rows = ndarray(NROW_NEIGH, NCOL)
i_row = 0

def coordinates(seq):
    return (0,0)

for seq in SeqIO():
    row, col = coordinates(seq) 
    latest_rows[row, col] = seq.sequence

